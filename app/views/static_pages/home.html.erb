<% if !anyone_signed_in? %>
  <div class="row">
    <%= link_to 'UsuO Moksleiviams!', new_student_session_path, { class: "home-link" } %>
  </div>
  <div class="row">
    <%= link_to 'UsuO Oranžiniams!', new_orange_session_path, { class: "home-link" } %>
  </div>
<% end %>

<h2>Aukštosios išsilavinimo įstaigos:</h2>
<%= render 'shared/institutions' %>